
*---------------------------------------------------------------------------------------------------------------------------------------------*
.
. Demo: K8s context On-Promise |  Monoem YOUNEB
.

*---------------------------------------------------------------------------------------------------------------------------------------------*

# 1. K8s Add Group context

# show context
kubectl config get-contexts

 => 	*******************************************************************************************
	* 	CURRENT   NAME                          CLUSTER      AUTHINFO           NAMESPACE *
	*         *	 kubernetes-admin@kubernetes   kubernetes   kubernetes-admin		  *
	*											  *	
	*******************************************************************************************

# Add cluster to 3 nodes (1 master + 2 workers)
cd .kube/
ls
scp root@192.168.1.40:/etc/kubernetes/admin.conf .
ls
export KUBECONFIG=~/.kube/config:~/.kube/admin.conf
kubectl config get-contexts
more admin.conf

*---------------------------------------------------------------------------------------------------------------------------------------------*
# 2. Change context
kubectl config use-context <context-name>

*---------------------------------------------------------------------------------------------------------------------------------------------*
# 3. Delete context
kubectl config delete-context <context-name>

*---------------------------------------------------------------------------------------------------------------------------------------------*
# 4. View all config
kubectl config view